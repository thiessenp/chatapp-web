/**
 * TODO
 *
 * SqlEngine
 * - loadDriver
 * - connect
 * - query
 *
 * postgreSQLDriver
 * - loadDriver
 * - query
 *
 * sqlQueries
 * - (this) functions(parms) e.g.
 * getAccountByUsername(username)
 * return `SELECT * FROM account WHERE username='${username}'`
 *
 * - or -
 *
 * - key:value strings, e.g.
 * getAccountByUsername: `SELECT * FROM account WHERE username='${username}'`
 */
